#include<bits/stdc++.h>
using namespace std;
int main()
{
      cin>>n>>m;
	    count = 0;
	    vector<vector<ll> > mat(n, vector<ll> (m)), ans(n, vector<ll> (m, -1));
	    vector<ll> flagr(n, 0), flagc(m, 0);
      return 0;
}
